import type { WakmenuSnapshot } from '@stream/wakmenu'
const DB = 'stream-wakmenu'; const STORE = 'state'; const KEY = 'snapshot'
export async function loadGame(): Promise<Partial<WakmenuSnapshot> | undefined> { if (typeof indexedDB === 'undefined') return undefined; return new Promise((resolve) => { const request = indexedDB.open(DB, 1); request.onupgradeneeded = () => request.result.createObjectStore(STORE); request.onerror = () => resolve(undefined); request.onsuccess = () => { const tx = request.result.transaction(STORE, 'readonly').objectStore(STORE).get(KEY); tx.onsuccess = () => resolve(tx.result as Partial<WakmenuSnapshot> | undefined); tx.onerror = () => resolve(undefined) } }) }
export async function saveGame(snapshot: WakmenuSnapshot): Promise<void> { if (typeof indexedDB === 'undefined') return; await new Promise<void>((resolve) => { const request = indexedDB.open(DB, 1); request.onupgradeneeded = () => request.result.createObjectStore(STORE); request.onerror = () => resolve(); request.onsuccess = () => { const tx = request.result.transaction(STORE, 'readwrite'); tx.objectStore(STORE).put(snapshot, KEY); tx.oncomplete = () => resolve(); tx.onerror = () => resolve() } }) }
