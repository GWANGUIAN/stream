{
  "name": "@stream/session",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./file-store": "./src/file-store.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"require('node:fs').rmSync('.turbo',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@stream/auth": "workspace:*",
    "@stream/core": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@stream/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
